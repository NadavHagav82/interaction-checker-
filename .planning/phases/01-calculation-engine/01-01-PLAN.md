---
phase: 01-calculation-engine
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - dovetail-calc/package.json
  - dovetail-calc/vite.config.ts
  - dovetail-calc/tsconfig.json
  - dovetail-calc/tsconfig.app.json
  - dovetail-calc/tsconfig.node.json
  - dovetail-calc/src/types/dovetail.ts
autonomous: true
requirements:
  - INP-03
  - INP-04
  - CALC-06

must_haves:
  truths:
    - "npm run test:run executes Vitest successfully with 0 test files (no failures)"
    - "TypeScript strict mode is enabled and tsc --noEmit passes"
    - "DovetailInput and DovetailResult interfaces are importable from src/types/dovetail.ts"
    - "fraction.js is installed and importable"
  artifacts:
    - path: "dovetail-calc/package.json"
      provides: "Project manifest with test scripts and fraction.js dependency"
      contains: "vitest"
    - path: "dovetail-calc/vite.config.ts"
      provides: "Vite + Vitest configuration with node environment"
      contains: "environment: 'node'"
    - path: "dovetail-calc/src/types/dovetail.ts"
      provides: "Shared TypeScript interfaces for calculator input/output"
      exports: ["DovetailInput", "DovetailResult"]
  key_links:
    - from: "dovetail-calc/vite.config.ts"
      to: "vitest"
      via: "test block configuration"
      pattern: "test:.*environment.*node"
    - from: "dovetail-calc/src/types/dovetail.ts"
      to: "calculator constants"
      via: "SOFTWOOD_RATIO, HARDWOOD_RATIO, MIN_PIN_WIDTH_INCHES exports"
      pattern: "export const (SOFTWOOD_RATIO|HARDWOOD_RATIO|MIN_PIN_WIDTH_INCHES)"
---

<objective>
Scaffold the Vite + React + TypeScript project, install dependencies (fraction.js, Vitest), configure Vitest for Node-environment testing, and create the shared TypeScript interfaces that Plans 02 and 03 depend on.

Purpose: All subsequent plans need a working project with test runner and shared types. This is the foundation that unblocks parallel development of calculator+formatter (Plan 02) and inputParser (Plan 03).

Output: A buildable Vite project at `dovetail-calc/` with Vitest passing (0 tests), fraction.js installed, and `src/types/dovetail.ts` exporting `DovetailInput`, `DovetailResult`, and ratio/angle constants.
</objective>

<execution_context>
@C:/Users/NadavNew/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/NadavNew/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-calculation-engine/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold Vite project and install dependencies</name>
  <files>
    dovetail-calc/package.json
    dovetail-calc/vite.config.ts
    dovetail-calc/tsconfig.json
    dovetail-calc/tsconfig.app.json
    dovetail-calc/tsconfig.node.json
  </files>
  <action>
    Run `npm create vite@latest dovetail-calc -- --template react-ts` from the project root (C:/Users/NadavNew).

    Then inside `dovetail-calc/`:
    1. Run `npm install`
    2. Run `npm install fraction.js`
    3. Run `npm install -D vitest @vitest/coverage-v8`
    4. Update `vite.config.ts` to add the Vitest test configuration block:
       - Add `/// <reference types="vitest/config" />` at top
       - Add `test: { environment: 'node', globals: true, include: ['src/**/*.test.ts', 'src/**/*.spec.ts'] }` inside defineConfig
    5. Add test scripts to `package.json`:
       - `"test": "vitest"`
       - `"test:run": "vitest run"`
       - `"test:coverage": "vitest run --coverage"`
    6. Ensure `tsconfig.json` or `tsconfig.app.json` has `"strict": true`
    7. Create directory structure: `src/lib/`, `src/lib/__tests__/`, `src/types/`

    Per STATE.md decision: No separate scaffold phase -- this is the first plan within Phase 1.
    Per RESEARCH.md: Vitest 4 requires Vite >= 6; the `react-ts` template provides this.
  </action>
  <verify>
    <automated>cd C:/Users/NadavNew/dovetail-calc && npm run test:run 2>&1 | tail -5</automated>
    <manual>Vitest runs successfully with 0 test suites (no failures). npm run test:run exits 0.</manual>
  </verify>
  <done>Vite project scaffolded at dovetail-calc/, fraction.js and vitest installed, test runner configured for Node environment, `npm run test:run` exits cleanly.</done>
</task>

<task type="auto">
  <name>Task 2: Create shared TypeScript interfaces and constants</name>
  <files>
    dovetail-calc/src/types/dovetail.ts
  </files>
  <action>
    Create `src/types/dovetail.ts` with the following exports (per RESEARCH.md Pattern 1):

    1. Constants:
       - `export const SOFTWOOD_RATIO = 8;`  (1:8 slope for softwood)
       - `export const HARDWOOD_RATIO = 6;`  (1:6 slope for hardwood)
       - `export const MIN_PIN_WIDTH_INCHES = 3 / 16;`  (0.1875" structural minimum)

    2. `DovetailInput` interface:
       - `boardWidth: number` (inches, decimal)
       - `boardThickness: number` (inches, decimal)
       - `ratio: number` (6 for hardwood, 8 for softwood, or user override)
       - `pinCount?: number` (optional -- uses suggestPinCount if omitted)

    3. `DovetailResult` interface:
       - `pinCount: number` (number of tails)
       - `tailWidth: number` (inches, decimal -- width at face)
       - `pinWidth: number` (inches, decimal -- narrowest point)
       - `halfPinWidth: number` (inches, decimal -- narrowest point)
       - `tailDepth: number` (inches, decimal -- equals boardThickness)
       - `angle: number` (radians -- derived from Math.atan(1/ratio))
       - `pinWidthWarning: boolean` (true if pinWidth < MIN_PIN_WIDTH_INCHES)

    These types address:
    - INP-03: ratio field accepts hardwood/softwood selection
    - INP-04: ratio field accepts user override (any numeric value)
    - CALC-06: angle field is radians from atan(1/ratio), never hardcoded degrees

    Add JSDoc comments on each field explaining units and derivation.
  </action>
  <verify>
    <automated>cd C:/Users/NadavNew/dovetail-calc && npx tsc --noEmit 2>&1 | tail -5</automated>
    <manual>TypeScript compilation passes with no errors. The types file exports DovetailInput, DovetailResult, and all 3 constants.</manual>
  </verify>
  <done>src/types/dovetail.ts exists with DovetailInput, DovetailResult interfaces and SOFTWOOD_RATIO, HARDWOOD_RATIO, MIN_PIN_WIDTH_INCHES constants. tsc --noEmit passes.</done>
</task>

</tasks>

<verification>
1. `cd dovetail-calc && npm run test:run` exits 0 (Vitest configured, no test failures)
2. `cd dovetail-calc && npx tsc --noEmit` exits 0 (TypeScript compiles cleanly)
3. `fraction.js` appears in package.json dependencies
4. `vitest` appears in package.json devDependencies
5. `src/types/dovetail.ts` exports DovetailInput, DovetailResult, and 3 constants
</verification>

<success_criteria>
- Project at dovetail-calc/ builds and passes TypeScript checks
- Vitest runs in Node environment with no failures
- fraction.js is installed as a dependency
- Shared types are importable and will be consumed by Plans 02 and 03
</success_criteria>

<output>
After completion, create `.planning/phases/01-calculation-engine/01-01-SUMMARY.md`
</output>
